#include<iostream>
#include<cstring>
using namespace std;
const int MAX_N=1e5+7;
typedef long long ll;
ll v[MAX_N], c[MAX_N], mc[MAX_N];
ll dp[MAX_N][2];
int main()
{
  int n, queries;
  cin>>n>>queries;
  for (int i = 1; i <= n; i++) cin>>v[i];
  for (int i = 1; i <= n; i++) cin>>c[i];
  for (int q = 1; q <= queries; q++) {
    
  }
  return 0;
}
