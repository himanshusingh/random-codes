#include<iostream>
#include<cstdio>
#include<vector>
#include<algorithm>
using namespace std;

vector<int> idx[30010];

int main()
{
  int n;
  scanf ("%d", &n);
  for (int i = 1; i <= n; i++) {
    scanf ("%d", &x);
    idx[x].push_back(i);
  }
  for (int i = 1; i <= 30000; i++)
    for (int j = i+1; j <= 30000; j++)
  return 0;
}
